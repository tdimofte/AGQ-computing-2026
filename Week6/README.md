## Topological data analysis ##

Lecture notes and workshop notebooks.
If downloading the workshop files (rather than cloning the repository), please make sure to create a ```helper_functions/``` subdirectory (in your working directory) that contains the two python files ```noisy_circle.py``` and ```density.py```.
